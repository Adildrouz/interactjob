/**
 * /en/apply — English alias for the spontaneous application form.
 * Same component as /postuler; locale is "en" so useLocale() returns "en"
 * and the full English UI is shown automatically.
 */
export { default } from "@/app/[locale]/postuler/page";
